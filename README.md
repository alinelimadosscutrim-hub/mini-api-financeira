# Mini API Financeira

API simples para simular saldo e transações.

## Como rodar
1. `npm install`
2. `npm start`
3. Endpoints:
   - GET `/saldo`
   - GET `/transacoes`
   - POST `/depositar` { descricao, valor }
   - POST `/sacar` { descricao, valor }
